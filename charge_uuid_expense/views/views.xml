<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="charge_uuid_expense.list">
      <field name="name">charge_uuid_expense list</field>
      <field name="model">charge_uuid_expense.charge_uuid_expense</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="charge_uuid_expense.action_window">
      <field name="name">charge_uuid_expense window</field>
      <field name="res_model">charge_uuid_expense.charge_uuid_expense</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="charge_uuid_expense.action_server">
      <field name="name">charge_uuid_expense server</field>
      <field name="model_id" ref="model_charge_uuid_expense_charge_uuid_expense"/>
      <field name="state">code</field>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="charge_uuid_expense" id="charge_uuid_expense.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="charge_uuid_expense.menu_1" parent="charge_uuid_expense.menu_root"/>
    <menuitem name="Menu 2" id="charge_uuid_expense.menu_2" parent="charge_uuid_expense.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="charge_uuid_expense.menu_1_list" parent="charge_uuid_expense.menu_1"
              action="charge_uuid_expense.action_window"/>
    <menuitem name="Server to list" id="charge_uuid_expense" parent="charge_uuid_expense.menu_2"
              action="charge_uuid_expense.action_server"/>
    -->
  </data>
</odoo>